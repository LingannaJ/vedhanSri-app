apiVersion: v1
kind: Service
metadata:
  name: academy-service
  namespace: vedhansri-prod
spec:
  selector:
    app: academy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: ClusterIP